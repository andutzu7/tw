
/*
export var final=[];

let api_url = 'https://arcane-sierra-19327.herokuapp.com'
//valid_table_names = ['varste', 'medii', 'educatie', 'rata', 'judete']

let table_name = 'rata';

fetch(`${api_url}/${table_name}`)
    .then((response) => {
        return response.json();
    })
    .then((data) => {

        for (let row of data) {
            final.push(row);
        }
       console.log(final[1]['total']);
    });

*/